# 2026-09-18

## Astapb 仓库全面分析（只读，未改动源码）

对 D:\Personal\Astapb 做了完整结构梳理与代码审查。

**仓库定位**：个人嵌入式工程工具箱 + docsify 知识博客，GPL-3.0，
远程 EchoHeim/Astapb，部署域名 shilong.js.org，总量约 755 MB
（162 个 PDF 占大头，含考研/港股打新资料）。

**三层结构**
- `mfast` + `mfast.cfg` + `mfast_ui/`(8) + `mfast_fun/`(7)：交互式构建工具链 v1.0.6，
  bash 模块化，入口 source 全部脚本 + `do_action` 统一调度。
- `shell/`：H616 / mp157 / RPi / rootfs 四个平台的实战脚本。
- `docs/`(docsify) + `src/`(6 个 C 驱动样例) + `scripts/`(Python 报 IP) + `Records.md`。

**发现的缺陷（详见当日对话）**
1. `mfast_ui/menu_rootfs.sh` 与 `mfast_fun/fun_rootfs.sh` 重复定义 rootfs_ui/rootfs_menu，
   后者覆盖前者 → Armbian 菜单是死代码，且 UI 显示 "Armbian" 与实现 "H616" 不符。
2. 主菜单 1 走的 rootfs_menu 调用 `H616_build` / `update_all`，两者均未定义 → 必然报错。
3. `menu_rootfs.sh` 引用未定义变量 `BB4U_STATUS`、`DISPLAY_SYS_UPDATE`、函数 `back_footer`。
4. `menu_mp157.sh` UI 里的第 7、8 项（compile/clear rootfs）没有对应 case 分支。
5. `menu_RPi.sh` 第 2/3/4 项误调用 H616 的构建函数；`build_rpi`/`build_pi3_*`/`build_pi4_*` 是死代码。
6. `mp157_cpmpile_rootfs` 引用未定义变量 `PATH_SOURCE_5_4_BUSYBOX`。
7. 分层混乱：`settings.sh`（UI 性质）放在 `mfast_fun/`，`change_path`（逻辑）放在 `mfast_ui/`。
8. 配置三处分散：mfast.cfg / mfast_fun/path.sh / 硬编码 `$HOME` 路径；
   `sed -i` 直接改写 mfast.cfg 会污染版本控制。
9. `mp157_flash` 的 factory/bootfs/rootfs 三分支赋值完全相同，逻辑未实现差异。
10. `docs/index.html` 本地样式路径写成 `../css/custom.css`，正确应为 `css/custom.css`。
11. git 历史提交信息存在编码乱码（GBK/UTF-8 混用）。
12. `CatchIP.py` 的 `finally: s.close()` 在 socket 创建失败时会抛 UnboundLocalError。
13. 依赖 `dpkg-query`，非 Debian 系主机无法启动；`mfast_backup` 直接 push master。

---

## 第二轮：仓库重组后的现状复核 + 多站点同仓方案（只读 + 少量清理）

复核时发现仓库已被提交 `39bbf28`「文件结构重组」改造：**mfast 工具链、shell/、src/、Records.md 全部删除**，
新增 `CodeKey/`（公众号素材库）与 `WebSite/`（导航站）。旧分析结论中 1–9 条相关代码已不存在，仅存档。

**本地 master 领先 origin/master 1 个提交，尚未推送。**

### 多站点同仓（用户核心问题）
- GitHub Pages 一个仓库只能发布 **1 个站点**，且只绑 **1 个自定义域名** → `docs/` 与 `WebSite/` 无法各自独立发布。
- 现行 `docs/CNAME` = shilong.js.org，说明发布源是 master 的 `/docs` → `WebSite/` 根本不会被发布。
- 方案 A（推荐）：改用 Actions 构建单一产物，博客放根/`blog/`、导航放 `/aa/`，一次部署；CNAME 放产物根。
- 方案 B：保持两个独立仓库（`AnywhereAnything` 实测仍在线）。
- `.github/workflows/publish` 无扩展名 → Actions 永不触发，目前生效的只有"从分支发布"。

### 平台事实（实测）
- jsDelivr 对 `EchoHeim/Astapb` **正常服务**（custom.css / logo.png / search.js 均 200）；限制为单文件 20 MB、包 50 MB。
- GitHub 单文件 >100 MB 拒绝；当前最大单文件 81.46 MB（考研大纲解析）。
- `shilong.js.org` 与 `echoheim.github.io/AnywhereAnything/` 当前均 200。

### 风险与卫生问题
- **版权**：`docs/blog/尚德机构-考研/` 134 个付费课程资料、`港股打新/` 28 个 PDF → 公开托管侵权风险（DMCA 会牵连整仓）。
- **体积**：docs 415.5 MB / .git 356.8 MB；**git 历史不可回收**，删文件不减仓库，需 filter-repo 重写历史。
- **许可错配**：全仓 GPL-3.0 覆盖文章与素材不合适，应拆分（代码 / CC BY-NC-SA / 保留所有权利）。
- **隐私**：邮箱与 GitHub 账号公开；已删除文件的内容仍留在 git 历史可被翻出。
- **WebSite 图标缺失**：`icon/digital/`、`icon/net/`、`icon/others/` 三个目录为空，但 main.js 的"数码/网络/其他/工具"分类要从这些目录取图 → 图标 404。
- `WebSite/README.md` 部署地址过期；`WebSite/.workbuddy/memory/2026-09-18.md` 是 0 字节空文件被提交。
- 根 `README.md` 仍是 MFAST 旧简介，与实际内容完全脱节。
- 中文文件名：`06de1695qq5d7c-...`、`...45219-...` 与 `...4529-...` 疑为手抖产生的脏名。

### 本次实际改动（仅两处）
1. 删除自己产生的临时扫描文件（`.workbuddy/_scan*.txt`、`_r*.txt`、`_urls*.txt`）——其中 `_scan*.txt` 已被上一提交误纳入版本控制。
2. `.gitignore` 追加 `.workbuddy/_*`，防止此类临时文件再次被提交。
未改动任何业务代码、未执行任何 git 写操作（无 commit / 无 push）。

---

## 第三轮：执行方案 A + 修复 workflow + 建立分层许可（已落地文件改动）

### 1. 方案 A 落地：双站点合并为单一 GitHub Pages 产物
- 新增 `.github/workflows/publish.yml`：`build` 作业把 `docs/.` 复制到产物根（博客 →
  `https://shilong.js.org/`），把 `WebSite/.` 复制到 `_site/aa/`（导航 →
  `https://shilong.js.org/aa/`），并清理 `_site/aa/.workbuddy`、`_site/aa/README.md`，
  写入 `_site/CNAME` 与 `_site/.nojekyll`；`deploy` 作业用
  `actions/configure-pages@v5` + `upload-pages-artifact@v3` + `deploy-pages@v4`。
- 删除旧的无扩展名文件 `.github/workflows/publish`（Actions 从不识别它）。
- 博客选择放在**产物根**而非 `/blog/`，以保持现有 URL 与访客链接不变。
- `docs/index.html`：`../css/custom.css` → `css/custom.css`。
  注：原写法在根路径下因 URL 解析会被钳制到根而**恰好可用**，但一旦博客移到子路径即失效。
- 已验证：workflow YAML 可被 PyYAML 正常解析，jobs/permissions/steps 结构正确。
- **部署体积提醒**：产物约 415 MB（考研 PDF 占绝大部分），每次 push 都会重传。
  workflow 内已预留注释掉的一行，可排除两个第三方资料目录，同时减少约 400 MB。

### 2. 分层许可体系（根 LICENSE 为总览，区域 LICENSE 优先）
新建：`LICENSES/MIT.txt`、`LICENSES/CC-BY-NC-SA-4.0.md`，
以及 `docs/LICENSE`、`docs/blog/尚德机构-考研/LICENSE`、`docs/blog/港股打新/LICENSE`、
`CodeKey/LICENSE`；重写根 `LICENSE` 与 `WebSite/LICENSE`（原均为 GPL-3.0）。
- 代码（scripts、.github、站点代码）→ MIT
- 原创文章与图片 → CC BY-NC-SA 4.0
- `CodeKey/` → 保留所有权利
- `WebSite/icon|header|search/` → 第三方商标，仅作链接索引
- 两个第三方资料目录 → 明确"未授权、不适用任何开放许可"，并附权利人移除通道
- GPL-3.0 整体退役（它属于已删除的 mfast 工具链），历史提交不受影响

### 3. 未处理（按用户要求保留）
README.md 与现状脱节、WebSite 三个空图标目录导致 404、`WebSite/.workbuddy` 空文件、
考研/港股资料仍在公开仓库、git 历史体积未清理、本地领先 origin 1 个提交未推送。
