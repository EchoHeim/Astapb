# 2026-09-22

## aa 导航站点补录（WebSite）

- 需求：参考 `lackar.com/aa` 补充导航内容，去掉重复网址，不删除现有网址。
- **原站已整站下线**（DreamHost `Site not found`），Wayback Machine 在本机网络下 502。
  最后从 **Common Crawl `CC-MAIN-2019-18`（2019-04-22）** 的 `lackar.com/aa/` 快照里
  拿到完整 HTML，解析出 17 个分类 / 约 250 条的原始清单。
  取证细节写进了 `docs/aa-import.md`（含 Range 取 WARC 记录 + zlib 解压的复现步骤）。
- 结果：**97 → 259 条 / 20 个分类**（新增 162 条）。
  - 新建 aa 独有的 `电影` / `应用` / `搜索` 三个分类，排在锚点最后，原有 17 个分类顺序不动。
  - 三个新推荐位图标：`header/appstore.png`（180²，取自 apps.apple.com 的 apple-touch-icon）、
    `header/doubanmovie.png`（100²，与豆瓣主站同一个 favicon，复用已有资源）、
    `header/google.png`（100²，复用已有资源）。
- 去重按**域名**做（忽略 `www.`，子域算不同站），命中 3 条：Kindle 书店、豆瓣相册、Fast Company。
- 原有 97 条**一条没动** —— 用脚本从 git HEAD 取出原文件逐条比对，确认分类/顺序/名称/URL/图标键全等同。
  新增条目统一追加在各分类 `sites` 末尾并带 `// ---- 以下为 aa 补录 ----` 分隔注释。
- 跳过关停 / 已改名 / 非站点（只是搜索模板）的条目 30 余项，清单和理由在 `docs/aa-import.md`。
- 新增条目绝大多数没有本地图标 → 首字母色块兜底（有图标 75 / 兜底 184）。
  刻意不套别家 Logo，与项目既有取向一致。
- 同步更新：`index.html` 的 meta 描述、`render.ts` 注释、README 新增「内容来源」一节。
- 验证：`npm run build` 通过（20 分类 / 259 站点，图标与数据一致）；
  用 CDP 截图核对了桌面深浅两套主题 + 390px 移动端。

### 踩到的坑

- **同一条消息里对同一个文件发多个 Edit 会互相覆盖**：三处改动只有两处生效，
  且工具都返回成功。同一文件的编辑必须串行，改完要 grep 复核。
